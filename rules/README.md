# `rules/`
This directory contains configuration files for project-wide standards, such as linting rules (`.editorconfig`, `analysis_options.yaml`, `.globalconfig`), Git hooks, and IDE-specific enforcing settings.

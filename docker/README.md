# `docker/`
This directory contains the `docker-compose.yml` for local development orchestration, as well as production-ready, multi-stage `Dockerfile` definitions for all microservices and frontend apps.
